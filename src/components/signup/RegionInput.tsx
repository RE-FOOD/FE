// import { TextInput, View } from 'react-native';
// import { TouchableOpacity } from 'react-native-gesture-handler';

// type Props = {
//   region: string;
//   onChangeRegion: (val: string) => void;
//   onTouchStart: (val: boolean) => void;
// };

// const RegionInput = ({ region, onChangeRegion, onTouchStart }: Props) => {
//   return (
//     <TouchableOpacity
//       onPress={() => onTouchStart(true)}
//       // onChangeText={(text) => {
//       //   onChangeRegion(text);
//       // }}
//     >
//       <View pointerEvents="none">
//         <TextInput value={region} placeholder="주소 검색" editable={false} />
//       </View>
//     </TouchableOpacity>
//   );
// };

// export default RegionInput;
