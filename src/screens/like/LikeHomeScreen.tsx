import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const LikeHomeScreen = () => {
  return (
    <SafeAreaView>
      <Text>Like Home Screen</Text>
    </SafeAreaView>
  );
};

export default LikeHomeScreen;
